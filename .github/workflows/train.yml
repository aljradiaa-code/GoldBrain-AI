name: GoldBrain Train & Export

on:
  push:
    paths:
      - 'data/**'
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install exactly compatible versions
        run: |
          pip install tensorflow==2.13.0
          pip install protobuf==3.20.3
          pip install jax==0.4.13 jaxlib==0.4.13
          pip install tensorflowjs==4.8.0
          pip install pandas numpy h5py

      - name: Run training and export
        run: python scripts/train_model.py

      - name: Commit model
        run: |
          git config user.name "GoldBrain Bot"
          git config user.email "bot@goldbrain.ai"
          git add models/ gold_brain_weights.keras data/*.csv data/*.json || true
          git diff --quiet && git diff --staged --quiet || \
            git commit -m "Update model $(date '+%Y-%m-%d %H:%M')"
          git push
